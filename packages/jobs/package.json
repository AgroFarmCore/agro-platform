{
  "name": "@agro/jobs",
  "version": "0.1.0",
  "private": true,
  "description": "Queue names, job contracts and enqueue helpers foundation",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "lint": "prettier --check . --ignore-path ../../.prettierignore",
    "typecheck": "tsc --noEmit",
    "test": "node ../../tooling/scripts/noop.mjs test @agro/jobs"
  },
  "dependencies": {
    "bullmq": "^5.73.0",
    "zod": "^4.3.6"
  }
}
