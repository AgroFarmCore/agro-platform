# packages/auth

Purpose: shared auth and permission bootstrap for `portal` and `backoffice`.

Owner: `@team/backend` and `@team/platform`

Public API:

- role and access helpers
- auth configuration primitives

Allowed dependencies:

- `@agro/config`
- external auth libraries when introduced
