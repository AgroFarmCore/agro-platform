# packages/content

Purpose: content schemas, types, and query helpers for `apps/web`.

Owner: `@team/web`

Public API:

- content section models
- content queries and mappers

Allowed dependencies:

- `@agro/config`
- view-model-safe helpers only
