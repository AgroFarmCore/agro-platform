#!/usr/bin/env node

const [, , workspace = "workspace"] = process.argv;

console.log(`[foundation] ${workspace}: dev scaffold is not wired yet.`);
console.log(
  "Add real runtime setup before using this workspace in local development.",
);
