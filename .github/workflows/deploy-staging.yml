name: Deploy Staging

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: staging
    steps:
      - run: echo "Add staging deployment steps for web, portal, backoffice and worker"
