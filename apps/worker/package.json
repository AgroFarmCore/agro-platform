{
  "name": "@agro/worker",
  "private": true,
  "version": "0.1.0",
  "description": "Background jobs and automation worker",
  "scripts": {
    "dev": "node --run start:worker",
    "start:worker": "node --import tsx ./src/index.ts",
    "build": "tsc --noEmit",
    "lint": "prettier --check .",
    "typecheck": "tsc --noEmit",
    "test": "node ../../tooling/scripts/noop.mjs test @agro/worker"
  },
  "dependencies": {
    "@agro/config": "workspace:*",
    "@agro/jobs": "workspace:*",
    "@agro/modules": "workspace:*",
    "@agro/observability": "workspace:*",
    "bullmq": "^5.72.0",
    "ioredis": "^5.10.1",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "tsx": "^4.20.6"
  }
}
